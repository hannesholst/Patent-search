%h2
  Patent Search

=form_for @search, :url => searches_path, :html => {:id => 'search_form' } do |form| #, :remote => true
  =render :partial => "/shared/errors_for", :locals => {:resource => @search}
  =render :partial => "form", :locals => {:form => form}